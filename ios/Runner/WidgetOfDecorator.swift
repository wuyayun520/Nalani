
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let appElementAcrossCancelKey:String = "present top world servicegetDe"
fileprivate let kTriggerMsg:[Character] = ["v","i","c","e","I","D"]

/*: "getFirebaseID" :*/
fileprivate let app_disappearTitle:String = "raw selected modelgetF"
fileprivate let appIndicatorFormat:String = "eIDenvironment kit new float map"

/*: "getAreaISO" :*/
fileprivate let show_argumentMessage:String = "control receive disappeargetA"

/*: "getProxyStatus" :*/
fileprivate let notiNetPendingValue:[Character] = ["g","e","t","P","r","o","x","y"]
fileprivate let mainNumberryPath:String = "app"
fileprivate let showTimePath:[Character] = ["t","a","t","u","s"]

/*: "getMicStatus" :*/
fileprivate let main_backRegionDownUrl:String = "main output behavior capture wheregetM"
fileprivate let main_diskFormat:String = "argument package not else tingicStatus"

/*: "getPhotoStatus" :*/
fileprivate let constSmartId:String = "getPhwith contact create normal"
fileprivate let k_logUrl:String = "otoStfail take agent after response"

/*: "getCameraStatus" :*/
fileprivate let showTargetPath:String = "by script pendinggetCam"
fileprivate let showApplicationFormat:String = "eraStback operation empty"

/*: "reportAdjust" :*/
fileprivate let showStatisticalFormat:String = "contact transaction bridgereportAdj"
fileprivate let userQuantityName:[Character] = ["u","s","t"]

/*: "requestLocalPush" :*/
fileprivate let const_operationData:[Character] = ["r","e","q","u","e","s","t","L","o","c","a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let show_progressData:String = "getLapic your control capture"
fileprivate let show_tagStatusKey:[Character] = ["n","g","C","o","d","e"]

/*: "getTimeZone" :*/
fileprivate let show_promptData:String = "currency bar agent mirror transformgetTi"
fileprivate let mainPageKey:[Character] = ["n","e"]

/*: "getInstalledApps" :*/
fileprivate let kAfterMsg:String = "getInstalcurrent large display"
fileprivate let k_agentRawTitle:String = "ledAppsreduce top capture fatal"

/*: "getSystemUUID" :*/
fileprivate let constIndexUrl:String = "getSyinput option white top"
fileprivate let main_contextStr:String = "UIformat"

/*: "getCountryCode" :*/
fileprivate let k_listName:[Character] = ["g","e","t","C","o"]
fileprivate let kSystemFailureFormat:[Character] = ["u","n","t","r","y","C","o","d","e"]

/*: "inAppRating" :*/
fileprivate let userUpFormat:[Character] = ["i","n","A","p","p","R","a","t","i","n"]
fileprivate let const_trustData:String = "request"

/*: "apPay" :*/
fileprivate let mainObserverPath:String = "network false decide rangeapPay"

/*: "subscribe" :*/
fileprivate let show_enableName:String = "plainubplain"
fileprivate let noti_dropKey:[Character] = ["c","r","i","b","e"]

/*: "openSystemBrowser" :*/
fileprivate let mainFiftyContactValue:String = "point itemopenSyste"
fileprivate let showBounceValue:String = "mBrowserwhere of flexible net"

/*: "closeWebview" :*/
fileprivate let main_tabBackMsg:String = "CLOSE"

/*: "openNewWebview" :*/
fileprivate let noti_onPoorUrl:String = "optionen"
fileprivate let noti_forwardEmptyText:String = "nowew"

/*: "reloadWebview" :*/
fileprivate let app_activitySinceShowId:[Character] = ["r","e"]
fileprivate let mainPhotoFormat:String = "loadWfoundation world temp break target"

/*: "typeName" :*/
fileprivate let constTitleName:[UInt8] = [0x40,0x4d,0x44,0x51,0x7a,0x55,0x59,0x51]

/*: "deviceID" :*/
fileprivate let userScreenText:[UInt8] = [0x6c,0x6d,0x7e,0x71,0x6b,0x6d,0x51,0x4c]

fileprivate func cameraInput(start num: UInt8) -> UInt8 {
    let value = Int(num) - 8
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fireBaseID" :*/
fileprivate let userVisibleArrayMsg:[UInt8] = [0xb2,0xbd,0xa6,0xb1,0x96,0xb5,0xa7,0xb1,0x9d,0x90]

private func maxFrame(scheme num: UInt8) -> UInt8 {
    return num ^ 212
}

/*: "areaISO" :*/
fileprivate let showUsKey:[UInt8] = [0x4f,0x53,0x49,0x61,0x65,0x72,0x61]

/*: "isProxy" :*/
fileprivate let app_titleKey:[UInt8] = [0xc,0x16,0x35,0x17,0xa,0x1d,0x1c]

private func upScript(previous num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "langCode" :*/
fileprivate let k_acrossMessage:[UInt8] = [0x65,0x64,0x6f,0x43,0x67,0x6e,0x61,0x6c]

/*: "timeZone" :*/
fileprivate let notiBackgroundMessage:[UInt8] = [0x65,0x6e,0x6f,0x5a,0x65,0x6d,0x69,0x74]

/*: "installedApps" :*/
fileprivate let main_tingUrl:[UInt8] = [0x73,0x70,0x70,0x41,0x64,0x65,0x6c,0x6c,0x61,0x74,0x73,0x6e,0x69]

/*: "systemUUID" :*/
fileprivate let constEnterMessage:[UInt8] = [0x45,0x4f,0x45,0x42,0x53,0x5b,0x63,0x63,0x7f,0x72]

/*: "countryCode" :*/
fileprivate let mainOfSchemeKey:[UInt8] = [0x65,0x64,0x6f,0x43,0x79,0x72,0x74,0x6e,0x75,0x6f,0x63]

/*: "status" :*/
fileprivate let const_micMsg:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x73]

/*: "isAuth" :*/
fileprivate let mainCenterStr:[UInt8] = [0x9c,0x86,0xb4,0x80,0x81,0x9d]

private func allRight(drop num: UInt8) -> UInt8 {
    return num ^ 245
}

/*: "isFirst" :*/
fileprivate let constRevenueUrl:[UInt8] = [0xe8,0xf2,0xc5,0xe8,0xf1,0xf2,0xf3]

fileprivate func subjectMatter(protection num: UInt8) -> UInt8 {
    let value = Int(num) - 127
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: __LocalPush" :*/
fileprivate let showClearName:[Character] = ["_","_","L","o","c","a","l","P","u"]
fileprivate let showBridgeLayerContent:String = "ssource"

/*: "identifier" :*/
fileprivate let const_numberMessage:[UInt8] = [0x6c,0x61,0x60,0x6b,0x71,0x6c,0x63,0x6c,0x60,0x77]

/*: "HTTPProxy" :*/
fileprivate let mainTriggerSubMsg:String = "HTTlevel"

/*: "HTTPSProxy" :*/
fileprivate let user_remoteColorValue:[Character] = ["H","T","T","P","S","P","r","o","x"]
fileprivate let show_photoItemStr:String = "error"

/*: "SOCKSProxy" :*/
fileprivate let show_downName:[Character] = ["S","O","C"]
fileprivate let const_sincePicMessage:String = "break info optionKSProxy"

/*: "__SCOPED__" :*/
fileprivate let appFoundationTitleValue:String = "__SCOserver input create behavior from"
fileprivate let constFilterData:[Character] = ["P","E","D","_","_"]

/*: "tap" :*/
fileprivate let mainColorData:String = "ofp"

/*: "tun" :*/
fileprivate let showActiveFormat:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let userFlushPath:String = "ipsepresent"

/*: "ppp" :*/
fileprivate let appToFormat:String = "maxmaxmax"

/*: "Retry After or Go to " :*/
fileprivate let noti_pageFoundationData:[Character] = ["R","e","t","r","y"," ","A","f","t","e","r"," ","o","r"," ","G"]
fileprivate let k_putId:[Character] = ["o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let user_levelTitle:String = "search dismissFeedback"

/*: " to contact us" :*/
fileprivate let k_verticalWarningValue:String = "state album level to cont"
fileprivate let kRegionFormat:[Character] = ["a","c","t"," ","u","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WidgetOfDecorator.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let const_countMsg = (String(appElementAcrossCancelKey.suffix(5)) + String(kTriggerMsg)) // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let appRequestData = (String(app_disappearTitle.suffix(4)) + "irebas" + String(appIndicatorFormat.prefix(3))) // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let show_ratingTitle = (String(show_argumentMessage.suffix(4)) + "reaISO") // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let dataAdMoveMsg = (String(notiNetPendingValue) + mainNumberryPath.replacingOccurrences(of: "app", with: "S") + String(showTimePath)) // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let mainTargetMsg = (String(main_backRegionDownUrl.suffix(4)) + String(main_diskFormat.suffix(8))) // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let app_productionId = (String(constSmartId.prefix(5)) + String(k_logUrl.prefix(5)) + "atus") // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let showStatusAppTitle = (String(showTargetPath.suffix(6)) + String(showApplicationFormat.prefix(5)) + "atus") // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let user_errorKey = (String(showStatisticalFormat.suffix(9)) + String(userQuantityName)) // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let showOutputCameraMessage = (String(const_operationData)) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let appPlainValue = (String(show_progressData.prefix(5)) + String(show_tagStatusKey)) // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let userClientValue = (String(show_promptData.suffix(5)) + "meZo" + String(mainPageKey)) // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let appProgressMethodLevelTitle = (String(kAfterMsg.prefix(9)) + String(k_agentRawTitle.prefix(7))) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let main_complianceData = (String(constIndexUrl.prefix(5)) + "stemU" + main_contextStr.replacingOccurrences(of: "format", with: "D")) // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let constAllowStr = (String(k_listName) + String(kSystemFailureFormat)) // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let noti_transformStatusValue = (String(userUpFormat) + const_trustData.replacingOccurrences(of: "request", with: "g")) // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let dataArgumentTitle = (String(mainObserverPath.suffix(5))) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let constEnablelyName = (show_enableName.replacingOccurrences(of: "plain", with: "s") + String(noti_dropKey)) // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let noti_plusName = (String(mainFiftyContactValue.suffix(9)) + String(showBounceValue.prefix(8))) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let noti_operationData = (main_tabBackMsg.lowercased() + "Webview") // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let data_pendingKey = (noti_onPoorUrl.replacingOccurrences(of: "option", with: "op") + "NewWebv" + noti_forwardEmptyText.replacingOccurrences(of: "now", with: "i")) // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let data_backgroundPath = (String(app_activitySinceShowId) + String(mainPhotoFormat.prefix(5)) + "ebview") // 重载webView

//: struct JSMessageModel: HandyJSON {
struct TableTransformable: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: FocusHandyJSON?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct FocusHandyJSON: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension LacunaDelegate {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func represent(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = TableTransformable.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case const_countMsg:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = DelegateFormLeft.decide()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: userScreenText.map{cameraInput(start: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case appRequestData:
                //: AppWebViewController.getFireBaseToken { str in
                LacunaDelegate.add { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: userVisibleArrayMsg.map{maxFrame(scheme: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case show_ratingTitle:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = LacunaDelegate.size()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: showUsKey.reversed(), encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case dataAdMoveMsg:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = LacunaDelegate.tertiaryRevenue()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: app_titleKey.map{upScript(previous: $0)}, encoding: .utf8)!: status])

            //: case getLangCode:
            case appPlainValue:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: k_acrossMessage.reversed(), encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case userClientValue:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: notiBackgroundMessage.reversed(), encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case appProgressMethodLevelTitle:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: main_tingUrl.reversed(), encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case main_complianceData:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: constEnterMessage.map{$0^54}, encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case constAllowStr:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: mainOfSchemeKey.reversed(), encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case noti_transformStatusValue:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                LacunaDelegate.inwardPricingAppRequest()

            //: case apPay:
            case dataArgumentTitle:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.doingce(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: const_micMsg.reversed(), encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case constEnablelyName:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.doingce(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: const_micMsg.reversed(), encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case noti_plusName:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case noti_operationData:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.failOf()

            //: case openNewWebview:
            case data_pendingKey:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    LacunaDelegate.watch(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case data_backgroundPath:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.anyRoot()

            //: case getMicStatus:
            case mainTargetMsg:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                AllocatorViaVariable.shared.total { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: mainCenterStr.map{allRight(drop: $0)}, encoding: .utf8)!: auth, String(bytes: constRevenueUrl.map{subjectMatter(protection: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case app_productionId:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                AllocatorViaVariable.shared.fire { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: mainCenterStr.map{allRight(drop: $0)}, encoding: .utf8)!: auth, String(bytes: constRevenueUrl.map{subjectMatter(protection: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case showStatusAppTitle:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                AllocatorViaVariable.shared.objectObserver { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName, String(bytes: mainCenterStr.map{allRight(drop: $0)}, encoding: .utf8)!: auth, String(bytes: constRevenueUrl.map{subjectMatter(protection: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case user_errorKey:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        DelegateFormLeft.color(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        DelegateFormLeft.field(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case showOutputCameraMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: constTitleName.map{$0^52}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                LacunaDelegate.go(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension LacunaDelegate {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func watch(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = LacunaDelegate()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        SmallIsolateState.with()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func go(_ model: TableTransformable) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(data_targetMsg)" + (String(showClearName) + showBridgeLayerContent.replacingOccurrences(of: "source", with: "h"))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: const_numberMessage.map{$0^5}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func add(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func size() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func tertiaryRevenue() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if LacunaDelegate.by() || LacunaDelegate.thatLaunch() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func by() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(mainTriggerSubMsg.replacingOccurrences(of: "level", with: "PP") + "roxy")] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(user_remoteColorValue) + show_photoItemStr.replacingOccurrences(of: "error", with: "y"))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(show_downName) + String(const_sincePicMessage.suffix(7)))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func thatLaunch() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(appFoundationTitleValue.prefix(5)) + String(constFilterData))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((mainColorData.replacingOccurrences(of: "of", with: "ta"))) || keyStr.contains((String(showActiveFormat))) || keyStr.contains((userFlushPath.replacingOccurrences(of: "present", with: "c"))) || keyStr.contains((appToFormat.replacingOccurrences(of: "max", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func inwardPricingAppRequest() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func doingce(productId: String, source: Int = -1, payType: PriorityType, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        RectActivityContrast.thatShow()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        MethodBoundaryRequestDelegate.shared.begin(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            RectActivityContrast.administer()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    RectActivityContrast.listInfoAgent((String(noti_pageFoundationData) + String(k_putId)) + "\"" + (String(user_levelTitle.suffix(8))) + "\"" + (String(k_verticalWarningValue.suffix(8)) + String(kRegionFormat)))

                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.fiftyCentPiece()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
